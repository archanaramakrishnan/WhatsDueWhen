import React, { Component } from 'react';
import { Router, Route, Switch } from 'react-router-dom';

export const Login = () => {
    return(
        <div>
            <h1>Stuff</h1>
        </div>
    )
};